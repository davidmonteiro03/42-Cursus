clear
echo "=========== GIT STATUS ==========="
git status
echo "===========   GIT ADD  ==========="
git add .
echo "=========== GIT STATUS ==========="
git status
echo "=========== GIT COMMIT ==========="
git commit -m "git updates"
echo "============ GIT PUSH ============"
git push
