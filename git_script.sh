clear
echo "=========== GIT STATUS ==========="
git status
echo "============= GIT ADD ============"
echo "         adding files...          "
git add .
echo "=========== GIT STATUS ==========="
git status
echo "=========== GIT COMMIT ==========="
git commit -m "git updates"
echo "============ GIT PUSH ============"
git push
